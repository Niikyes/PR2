version: '3.8'
services:
  backend:
    build: ./backend
    container_name: backend_app
    ports:
      - "8080:8080"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: frontend_app
    ports:
      - "80:80"

